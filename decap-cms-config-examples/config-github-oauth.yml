# Decap CMS Configuration - GitHub OAuth
# This configuration uses GitHub OAuth directly (more technical setup)

backend:
  name: github
  repo: JaimeV365/xperience360  # ⚠️ UPDATE with your actual repo
  branch: main
  base_url: https://YOUR_OAUTH_GATEWAY_URL  # ⚠️ UPDATE with your OAuth gateway URL

# Media (images) configuration
media_folder: public/images/blog
public_folder: /images/blog

# Blog posts collection
collections:
  - name: "blog"
    label: "Blog Posts"
    folder: "content/blog"
    create: true
    extension: "md"
    format: "frontmatter"
    slug: "{{slug}}"
    summary: "{{title}}"
    sortable_fields: ["date", "title"]
    view_filters:
      - { label: "Date", field: "date", pattern: ".*" }
    fields:
      - { label: "Title", name: "title", widget: "string", required: true }
      - { label: "Publish Date", name: "date", widget: "datetime", required: true, format: "YYYY-MM-DD HH:mm:ss", date_format: "YYYY-MM-DD", time_format: "HH:mm:ss" }
      - { label: "Slug", name: "slug", widget: "string", required: true, hint: "URL-friendly version of title (e.g., my-blog-post)" }
      - { label: "Excerpt", name: "excerpt", widget: "text", required: false, hint: "Brief description for listings and SEO" }
      - { label: "Featured Image", name: "image", widget: "image", required: false, hint: "URL or path to image (e.g., /images/blog/post-image.jpg)" }
      - { label: "Author", name: "author", widget: "string", default: "Jaime Valle", required: true }
      - { label: "Categories", name: "categories", widget: "list", required: false, default: ["Blog"] }
      - { label: "Tags", name: "tags", widget: "list", required: false }
      - { label: "Body", name: "body", widget: "markdown", required: true }

# INSTRUCTIONS TO USE THIS CONFIG:
# 1. Set up GitHub OAuth App in your GitHub settings
# 2. Deploy an OAuth gateway (Vercel, Cloudflare Workers, etc.)
# 3. Update the 'repo' field above with your GitHub username/repo
# 4. Update the 'base_url' field with your OAuth gateway URL
# 5. Replace the contents of public/admin/config.yml with this file
# 6. Commit and push to GitHub
# 7. Visit https://xperience-360.com/admin to start using Decap CMS!
